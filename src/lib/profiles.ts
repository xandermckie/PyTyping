// Deprecated. The "profiles" concept was replaced by:
//   - lib/auth.ts    — local accounts + sessions (with hashed passwords)
//   - lib/backup.ts  — validated export/import + full data wipe
//   - context/SessionContext.tsx — active guest/account session
export {};
